# AI 营销视频生成与剪辑工作流 — 项目深度分析报告

> 分析对象：`marketing-video-agent-20260728-120611`（AI 营销数字员工通用包）
> 分析范围：架构设计、工作流、核心引擎实现、工程质量、风险与改进建议
> 分析日期：2026-08-03

---

## 一、项目定位：一句话概括

这是一套**"客户无关"的 AI 营销视频生产工作台**：本地 Agent 负责"想清楚"（顾问式引导、剧本共创、确认闸门），云端大模型（BasicRouter 网关）负责"画出来"（数字人视频、故事板图像），本地 ffmpeg/Remotion/HyperFrames 负责"剪出来"（运镜、字幕、拼接、质检交付）。

其最鲜明的三个设计取向：

1. **客户机零本地模型**——抠像、融合、渲染全部走云端 API，本地只做编排与纯 ffmpeg 合成；
2. **每一分钱都花在客户确认过的内容上**——脚本没定稿不出片、故事板没确认不出片，多道人工确认闸门硬阻断；
3. **Agent 无关的公共协议**——Kilo/Codex/Hermes 等宿主共用同一套 `BASICROUTER_*` 业务层，宿主只做薄适配。

---

## 二、整体架构分层

| 层级 | 组件 | 职责 | 关键设计 |
|---|---|---|---|
| 入口与引导层 | `agent_entry.py` / `setup_env.py` / `key_setup.py` | 环境自检、inline 自举、密钥闸门、client 识别 | 密钥经 stdin 传入、按会话隔离存储，不进参数/日志/文件 |
| 顾问共创层 | `AGENTS.md` + `skills/` 7 个 skill + `references/professional-storyboard-enrichment.md` | 八阶段共创漏斗（意图→素材→核心信息→风格→数字人形象→结构→逐段→渲染→定稿） | 引导在本地完成，可反复调整零成本 |
| 云端渲染层 | `br_client.py` → BasicRouter 网关 | 数字人视频（音画一体）、故事板/人物板图像 | 模型自动降级链 + 能力权威字段 `allowVideoType` |
| 本地合成层 | `remotion_engine.py` / `hf_engine.py` / `fuse.py` / `final_edit.py` | 运镜排版（Remotion）、字幕动效（HyperFrames）、拼接画中画（ffmpeg） | 引擎分工铁律：渲染归云端、运镜归 Remotion、字幕归 HyperFrames、拼接归 ffmpeg |
| 质检交付层 | `take_review.py` / `ocr_check.py` / `media_qc.py` / `pipeline.py` | take 评审、OCR 字幕残留检测、ffprobe QC、交付快照校验 | 全面 fail-closed：缺审批/缺证据即拒绝交付 |
| 状态契约层（横向） | `run_manifest.py` / `generation_ledger.py` / `artifact_contract.py` / `continuity_state.py` | 审批状态、append-only 账本、sha256 指纹绑定、断点恢复 | 文件变动即审批失效；跨宿主恢复同一 run |

### 模块依赖关系（简化）

```
pipeline ──→ run_manifest / storyboard / take_review / script_splitter
video_engine ──→ br_client / key_setup / seedance_prompt / artifact_contract
                 / generation_ledger / take_review / media_qc / ocr_check
storyboard ──→ br_client / artifact_contract / key_setup
final_edit ──→ remotion_engine        script_splitter ──→ video_segmentation
br_client / artifact_contract / project_utils ＝ 叶子基础层
```

---

## 三、端到端工作流剖析

正式流程是一条**带硬闸门的十二级流水线**（`pipeline.py` STAGES）：

```
brief → script → product_board → cast_board → product_usage
     → storyboard → render_plan → video → captions → final → delivery
```

### 关键机制

**1. 素材阶段化推进（禁止一次性生成）**
`storyboard.py --stage next` 逐阶段推进：产品图 → 产品九宫格板 →（确认）→ 人物六视图板 →（确认）→ 人物产品使用细节图 →（确认）→ 分段故事板 →（确认）→ 视频。产品板绑定源图指纹，源图一变旧确认即失效——这是对"陈旧产物复用"事故（INC-005）的制度化修复。

**2. 视频生成引擎（`video_engine.py`，2391 行）**
- **五种 videoType**：1=文生视频、2=首帧图生、3=首尾帧、4=单图参考数字人锚定（仅 kling 支持）、5=多图人景同框（seedance 支持）。
- **降级链**：`seedance-2.0 → kling-v3-omni-video → wan2.7-i2v`；能力判断优先信任网关返回的 `allowVideoType` 权威字段，硬编码 CAPS 表仅离线兜底。type=4 自动回落 kling，type=5 保持 seedance——细分能力而非一刀切。
- **真·并行 batch**：不是线程池，而是利用 API 异步性"先全部提交、再统一轮询"，N 段墙钟 ≈ 单段。
- **音画一体**：`kling-v3-omni-video` 一次调用产出带配音对口型的成片，无独立 TTS 后期。
- **幂等与恢复**：提交前写 submission intent（request_id 幂等），提交后落 manifest+ledger，支持断点恢复；正式模式强制 handoff 指纹匹配，不匹配即 `VIDEO_HANDOFF_MISMATCH` 拒绝。

**3. 故事板锚定体系**
- 人物板：六视图（全身正/后/侧 + 正脸正/后/侧），正脸特写为最高权重身份锚，针对 seedance ID 漂移的根因对策；
- 故事板：16:9、4×3、12 格电影级分镜（默认黑白铅笔风，彩色前置锚定由 `bw-storyboard-color-anchor` skill 负责）；
- 12 格转视频铁律：按格序生成连续视频，故事板作为主视觉参考（`--storyboard-ref`），防止"整图动画化"。

**4. 质检链（fail-closed）**
- **OCR 兜底**：出片后 macOS Vision 抽 5 帧检测画面文字，检出即 `delivery_blocked`；豁免必须登记到精确 `segment_id + take 指纹 + OCR 文本`；OCR 不可用时人工 clear 须绑定 ≥12 个唯一帧 SHA-256（含首尾帧）。
- **take 评审**：每段 formal take review，交付要求 overall ≥ 80 分。
- **交付快照**：`_delivery_snapshot()` 在交付前全量校验审批新鲜度、formal QC、OCR 状态，输出 sha256 交付身份。

---

## 四、工程质量评估

### 测试体系（55 个测试文件，成熟度高）

- **分层清晰**：单元测试（v13~v21 系列）→ 契约/闸门测试（handoff 指纹、OCR 闸门、fail-closed）→ e2e（`test_pipeline_delivery_e2e.py` 离线正式交付全链路，付费 API 明确排除在编排边界外）。
- **断言质量高**：如 "OCR 的 unavailable/error 绝不能当 clear"、"final 文件被篡改后交付验证必须抛 `FINAL_APPROVAL_REQUIRED_OR_STALE`"、"client/run_id 拒绝路径穿越"、"JSONL 账本容忍截断末行"。

### 真实事故文化（REAL_TEST_INCIDENTS.md，罕见亮点）

记录了 **12 起付费 API 实测事故**（非 mock），每起均有"根因 → 修复 → 回归测试"闭环，并沉淀 10 条严格执行规则。事故模式归类：

| 类别 | 代表事故 | 暴露的薄弱点 |
|---|---|---|
| 素材准入失控 | INC-001 | 未登记素材进入流程 |
| 接口/模型能力不一致 | INC-002/004/008/009 | 模型 ID 错位、imageCount 上限、最短时长 |
| 安全校验误伤 | INC-003 | 代理 peer 阻断合法下载 |
| 陈旧产物复用 | INC-005 | 旧故事板冒充新结果 → 指纹绑定修复 |
| 语义/契约缺失 | INC-006/007/010/011/012 | 角色混淆、静默丢失、降级掩盖能力不足 |

**共性模式是"静默降级/静默丢失"**——系统曾倾向容错继续而非阻断，修复方向统一转向 fail-closed。文末明确声明"旧成片不溯及既往地宣称合规"，工程诚实度罕见。

### 产物证据链（output/，完整可审计）

run 目录命名规律 `{client|用途}-{live|validation}-{日期}`；产物含分段 mp4 + 每段 QC JSON + 重试版本（`segment_03_v2`）+ batch_results + manifest + 字幕 spec + 最终成片（如 49.154 秒 9:16），重试件与首版并存，可完整追溯 OCR 返工过程。

---

## 五、核心设计亮点（值得借鉴）

1. **成本-确认对齐**：所有付费生成环节之前必有免费的本地的确认闸门，"每次生成都用在客户确认过的内容上"。
2. **能力权威字段优先**：模型能力不信硬编码表而信网关自描述（`allowVideoType`），硬编码仅离线兜底——对多模型网关场景是正确姿势。
3. **指纹即审批**：一切审批绑定 sha256 指纹，内容变即审批失效，从机制上杜绝"批了 A 交了 B"。
4. **事故驱动的规则沉淀**：12 起真实事故 → 10 条铁律 → 回归测试，文档（AGENTS.md）即运行规则的单一事实源。
5. **宿主无关协议**：`AGENT_ENTRY_PROTOCOL.md` 明确"禁止任何宿主基于 Agent 名授权/跳过/降级闸门"，跨宿主恢复只记录交接历史。

---

## 六、风险与改进建议

| 优先级 | 问题 | 建议 |
|---|---|---|
| 高 | **上帝文件**：`video_engine.py` 2391 行、`storyboard.py` 1839 行，提交/轮询/降级/OCR/持久化/CLI 多职责混杂 | 拆分为 submit/poll/model-pick/persist 子模块，CLI 层独立 |
| 高 | **静默吞错**：`_reference_is_trusted` 的 `except Exception: pass`；模型目录拉取失败静默退回空表 | 至少记 warning 日志并进 manifest，保留 fail-closed 语义但不静默 |
| 中 | **大量函数内延迟 import**（run_manifest、product_library 等），隐藏真实依赖、易循环引用 | 梳理依赖图，顶层 import + 必要时显式依赖注入 |
| 中 | **密钥管理**：本地文件存储（虽有权限控制），`save` 支持命令行参数传 key（仅警告） | 禁用参数传 key 或默认拒绝；考虑 OS keychain |
| 中 | **Seedance imageCount=1 能力上限**仍需人工选择端点或合成锚定图，不可自动绕过 | 在引导层显性提示，或在多图场景自动合成锚定图 |
| 低 | seed 一致性已证伪但参数保留（注释坦承"无害不报错"） | 移除或标记 deprecated，避免误导 |
| 低 | 交付体积：offline-assets 3290 文件 + remotion_engine 8028 文件（含捆绑 node_modules） | 评估拆分"核心包 + 按需下载引擎包" |

---

## 七、总评

**这是一个完成度和工程纪律都显著高于同类"AI 视频一键生成"项目的工作流系统。** 它没有被"生成能力"带跑，而是把主要复杂度投在了三件真正决定商用成败的事上：

1. **确认闸门体系**——控制付费生成的返工率；
2. **fail-closed 契约层**——指纹绑定 + 审批状态机 + append-only 账本，保证交付可审计；
3. **诚实的事故闭环**——12 起真实付费事故全部记录、修复、回归。

主要技术债集中在核心引擎的单文件体量与若干静默容错路径上，属于"能跑得好但维护成本在上升"的阶段。若按第六节建议做一轮引擎拆分与静默错误治理，这套工作流具备产品化交付给多个客户团队的成熟度。

**综合评分（主观，10 分制）**：架构设计 8.5 / 工程质量 8.5 / 文档与规则沉淀 9 / 可维护性 6.5 / 商业就绪度 8
