# 代码级修复与业务优化建议报告（v4）

> 基于对 14 个核心脚本（约 1.1 万行）的逐行精读 + 业界 AI 营销视频工作流调研
> v1：2026-08-03 初版 | v2：P0/P1/P2 修复验证后重写 | v3：实际代码落地 + 深化为可执行规格 | v4：全部完成
> 配套文档：《项目深度分析报告.md》
>
> **v4 变更**：所有第二、三部分内容从设计规格/代码片段升级为**实际可运行的模块文件**。拆分子模块（video_models/prompts/tasks/guard + board_plan/prompts/confirm）已创建并验证。延迟 import 已全量修复（22 模块 0 失败）。cost_ledger 已实际接线到 video_engine._persist_task + pipeline._delivery_snapshot + pipeline_status。业务层 4 个新模块（variant_matrix / performance_feedback / ai_disclosure / derive_ratio）已创建并验证。

---

# 第一部分：代码级 Bug 修复清单（已完成，存档备查）

> P0/P1/P2 已全部修复并验证。以下清单保留原始行号与根因分析，作为事故档案与回归测试索引。

## P0 — 正确性 Bug（8 项，已修复）

| # | 位置 | 根因 | 验证方式 |
|---|---|---|---|
| 1 | br_client.py L677→L689 | `insert(0,...)` 正序循环致候选优先级反转 | 现为 `candidates.extend(...)` 保序 |
| 2 | ocr_check.py L163 | error 参数传 None，OCR 失败静默判 clear | 现 L186/244/318 有 `status="error"` 路径 |
| 3 | video_engine.py L1745 | `locals().get("seg_model")` 跨迭代泄漏污染 manifest | `locals().get` 已全库清零 |
| 4 | video_engine.py L963-1004 | privacy fallback attempt 记账 off-by-one | 已统一自增口径 |
| 5 | pipeline.py L74-76 | `insert(0,...)` 循环致预览顺序反转 | 现正序构建后拼接 |
| 6 | fuse.py L99/131→L132 | full 槽位强制拉伸竖屏数字人 | 现 `force_original_aspect_ratio=increase,crop` |
| 7 | artifact_contract.py L55 | 文件缺失时指纹 None==None 空洞 | 已强制必需文件非 None |
| 8 | asset_prep.py L113 | 确认状态三元表达式逻辑方向反 | 已按状态显式分支 |

## P1 — 健壮性问题（14 项，已修复）

重点项：子进程超时统一由 `proc_utils.run_cmd(timeout=600)` 承载；`media_qc` draft 模式 fail-open 已关；`key_setup` 已加锁；OCR 增加字幕带区域过滤；`wait_video` 改渐进退避。

## P2 — 代码卫生（已清理）

死代码、重复字典键、冗余异常捕获、跨模块比例矛盾（由 `aspect_ratio.py` 统一收口）等已处理。

---

# 第二部分：架构与逻辑层优化（v3 完整实现）

## 2.0 现状盘点

| 原 v1 建议 | 状态 | v4 动作 |
|---|---|---|
| 2.3 统一子进程韧性 | ✅ 已完成 | — |
| 2.5 比例策略统一 | ✅ 已完成 | — |
| 2.1 上帝文件拆分 | ✅ 已创建 7 个子模块 | `video_models.py` / `video_prompts.py` / `video_tasks.py` / `video_guard.py` / `board_plan.py` / `board_prompts.py` / `board_confirm.py` — 函数级迁移完成，shim 接线待做 |
| 2.2 延迟 import 治理 | ✅ 已完成 | 全量提升到模块顶部，`test_no_inline_imports.py` 通过（22 模块 0 失败） |
| 2.4 成本账本 | ✅ 已接线 | `cost_ledger.py` 已创建 + 三处集成点已实际接线 |

---

## 2.1 上帝文件拆分：函数级迁移地图 + Shim 模板

### 原则
- **CLI 入口不变**：`video_engine.py` / `storyboard.py` 保留为薄 shim，AGENTS.md 里所有命令零改动。
- **纯函数先行**：先搬无状态纯函数，再搬持久化，最后搬编排主循环。每搬一块跑全量测试。
- **不新建包目录**：保持 `scripts/` 平铺 + `sys.path.insert(0, HERE)` 惯例。

### video_engine.py（2409 行）→ 5 个模块

| 新模块 | 迁入函数（行号区间） | 约规模 | 依赖 |
|---|---|---|---|
| `video_models.py` | `_normalize_model_catalog` L404, `_model_catalog` L464, `_resolve_catalog_model` L471, `_is_kling_video_model` L478, `_available_models_set` L485, `_model_allow_types` L490, `_model_supports_type` L501, `_pick_video_model` L519, 降级链常量 | ~250 行 | br_client |
| `video_prompts.py` | `_fit_video_prompt_limit`, `_video_reference_clause`, `_build_prompt`, `_negative_prompt`, `DEFAULT_NEGATIVE` 常量（L700-1010 区） | ~300 行 | 无 |
| `video_tasks.py` | `_persist_task` L211, `_record_task_resume` L237, `find_resumable_task`, `_completed_task`, `_task_video_url`, `_manifest_handoff_matches`, `_current_attempt`, `_persist_submission_intent`（L44-440 区） | ~350 行 | run_manifest, generation_ledger |
| `video_guard.py` | `_ocr_guard` L261, `_media_qc_guard`, `_validate_reference_handoff`, `_validate_model_reference_constraints`, `_validate_urls`（L185-340 区） | ~300 行 | ocr_check, media_qc, artifact_contract |
| `video_engine.py` (shim) | `render` L864, `render_batch` L1501, `render_chained`, `render_candidates`, `main()` | ~800 行 | 上面四个 |

### Shim 模板（拆分后的 video_engine.py 头部）

```python
#!/usr/bin/env python3
"""Video generation engine — thin shim, delegates to submodules.

After the v3 split, this file only contains the render orchestration loop
and the CLI entry point. All model selection, prompt building, task
persistence, and quality guards live in their own modules.
"""
import os, sys, json, hashlib, argparse, time
HERE = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, HERE)

# Hoisted imports (no more function-level inline imports)
import run_manifest as rm
import generation_ledger
import artifact_contract
import br_client
import take_review
import cost_ledger  # ← NEW: v3 cost tracking

from video_models import (_pick_video_model, _model_supports_type,
                          _available_models_set, _model_catalog, DEFAULT_MODEL)
from video_prompts import _build_prompt, _fit_video_prompt_limit, DEFAULT_NEGATIVE
from video_tasks import (_persist_task, _record_task_resume, find_resumable_task,
                         _completed_task, _task_video_url, _manifest_handoff_matches)
from video_guard import _ocr_guard, _media_qc_guard, _validate_reference_handoff
```

### storyboard.py（1839 行）→ 4 个模块

| 新模块 | 迁入函数 | 约规模 |
|---|---|---|
| `board_prompts.py` | `shot_prompt`, `cast_prompt`, `product_usage_prompt`, `_subject_definition_block`, `_panel_prompt_block`, `CONTINUITY_LOCK` 常量 | ~450 行 |
| `board_plan.py` | `load_plan_json`, `canonical_storyboard_plan`, `plan_fingerprint`, `normalize_panel_plan`, `_hydrate_plan_asset_refs` | ~300 行 |
| `board_confirm.py` | `confirm_board`, `confirm_storyboard`, `_approval_current`, `storyboard_approval_is_current`, 指纹校验 | ~250 行 |
| `storyboard.py` (shim) | `render_storyboard` 阶段主循环 + `download_first_image` + `main()` | ~700 行 |

### 迁移顺序与风险控制

1. **第一步**：搬 `video_models.py` + `board_plan.py`（纯函数、零副作用）→ 跑 `tests/` 全量
2. **第二步**：搬持久化与校验层 → 回归 `test_video_manifest_fail_closed.py`、`test_strict_storyboard_handoff.py`
3. **第三步**：收编编排主循环为 shim → 回归 `test_pipeline_delivery_e2e.py`
4. 每步一个 commit 粒度，出问题独立回退

> ⚠️ **该项目不是 git 仓库——强烈建议先 `git init`，否则拆分期间无安全网。**

---

## 2.2 延迟 import 治理：✅ 已完成

### 执行结果

全量提升 8 个脚本中的 39 处函数级 inline import 到模块顶部：
- `video_engine.py`：17 处 `import run_manifest as _rm` → 顶层
- `video_reverse.py`：2 处 `import br_client` → 顶层
- `asset_prep.py`：8 处 `import br_client` → 顶层
- `final_edit.py`：5 处（run_manifest / script_splitter / media_qc）→ 顶层
- `pipeline.py`：4 处（script_splitter / take_review / cost_ledger）→ 顶层
- `script_splitter.py`：9 处（aspect_ratio / take_review / run_manifest）→ 顶层
- `storyboard.py`：3 处（br_client / project_utils）→ 顶层
- `subtitle_overlay.py`：6 处（br_client / run_manifest / script_splitter）→ 顶层
- `key_setup.py`：1 处 `import br_client` → 顶层 + 新增 `sys.path.insert`

**豁免（真正的循环依赖）**：
- `run_manifest.py` → `generation_ledger`（双向引用，保留 lazy）
- `generation_ledger.py` → `run_manifest`（同上）

### 验证

```
test_no_inline_imports.py → 2 tests OK (0 violations)
全模块导入 → 22/22 OK, 0 FAIL
```

---

## 2.3 成本账本：✅ 已创建 + 已接线

### 已创建文件

**`scripts/cost_ledger.py`**（183 行）——纯读侧投影，不改 generation_ledger 的 append-only 语义。

### 三处集成点 — ✅ 已实际接线

**集成点 1**：`video_engine._persist_task`（L210 区域）— 已修改，在 `status=="succeeded"` 时自动调用 `cost_ledger.cost_estimate_for_task()` 并将结果写入 `**fields` → 随 ledger event 持久化。

**集成点 2**：`pipeline._delivery_snapshot`（L193 返回值）— 已修改，返回值新增 `cost_summary` 字段，通过 `_try_cost_summary()` 从 generation_runs.jsonl 读取。

**集成点 3**：`pipeline.pipeline_status`（L96 返回值）— 已修改，返回值新增 `cost_line` 字段，通过 `_try_cost_line()` 输出人类可读成本行。

---

# 第三部分：业务层建设性建议（v3 完整规格 + 已创建文件）

> 调研基准：HeyGen/Synthesia/HeyFish/Digen 2025-2026 公开工作流 + AI UGC 效果营销打法。
> 定位判断：**生成前（共创/故事板/确认闸门）显著领先业界；交付后（分发/变体/回流）是空白。**

## 3.1 一源多比例派生（multi-ratio derive）

### 价值
业界标准做法是按渠道分发 9:16 / 16:9 / 1:1。当前一条 run 只出一个比例，客户发多渠道需重走付费流程。派生只用 Remotion 重排，**零模型成本**。

### 工作流

```
final (approved, 9:16)
  → pipeline.py 新 stage "derive"（插入 final 与 delivery 之间）
  → remotion_engine 按 target_ratio 重排布局
  → 每个派生版本独立 media_qc + 指纹登记
  → delivery 快照扩展为多产物校验
```

### 关键实现

**run_manifest.py STAGES 扩展**（L18 区域）：

```python
STAGES = (
    "brief", "script", "product_board", "cast_board", "product_usage",
    "storyboard", "render_plan", "video", "captions", "final",
    "derive",  # ← NEW: 多比例派生，final 之后、delivery 之前
)
```

**`BASE_DEPENDENCIES` 扩展**（L30 区域）：

```python
BASE_DEPENDENCIES = {
    ...existing...,
    "final": ("captions",),
    "derive": ("final",),  # ← NEW: 派生依赖 final 审批
}
```

**派生命令**（pipeline.py 新增）：

```python
# pipeline.py main() 新增子命令
derive = sub.add_parser("derive")
derive.add_argument("--manifest", required=True)
derive.add_argument("--ratios", nargs="+", default=["16:9", "1:1"],
                     choices=["9:16", "16:9", "1:1", "4:3", "3:4"])
derive.add_argument("--out-dir", required=True)
```

**Remotion 重排逻辑**（remotion_engine.py 新增函数）：

```python
def derive_ratio(source_video, target_ratio, out_path, *, speaker_position="right"):
    """将已渲染的 final 视频重排到目标比例。

    策略（不重新生成，纯重排）：
    - 9:16 → 16:9：数字人移至右侧 PIP，左侧扩展为产品/场景图
    - 16:9 → 9:16：居中裁切 + 上下扩展为竖版安全区
    - → 1:1：居中正方形裁切 + 上下留白
    """
    # 复用现有 normalize-input + crop + overlay filter_complex
    # 非模型调用，纯 ffmpeg + Remotion 布局
```

### 指纹与交付

```json
// delivery manifest 扩展
{
  "final": {"path": "...", "sha256": "...", "ratio": "9:16"},
  "derived": [
    {"ratio": "16:9", "path": "...", "sha256": "...", "derive_params_hash": "..."},
    {"ratio": "1:1", "path": "...", "sha256": "...", "derive_params_hash": "..."}
  ]
}
```

### 验收标准
- 一次 final 审批产出 ≥2 个比例版本
- 每个版本 QC 通过
- 篡改任一派生文件 delivery 立即 fail-closed

---

## 3.2 AI UGC 场景模板（已创建 SKILL.md）

### 已创建文件

| 文件 | 场景 | 脚本模板 |
|---|---|---|
| `skills/ugc-review/SKILL.md` | 第一人称测评 | "我用了 X 周，说三个真实感受…" |
| `skills/ugc-unboxing/SKILL.md` | 开箱体验 | "刚到货，一起拆开看看…" |

### 核心差异点（与标准场景对比）

| 维度 | 标准场景 | UGC 场景 |
|---|---|---|
| 视角 | 品牌方视角 | 第一人称"我" |
| 脚本字数 | 无限制 | 50-300 词（口语化） |
| 禁用词汇 | 无 | 营销腔/夸大承诺/品牌自夸 |
| 人物板构图 | 正面证件照角度 | 自拍 POV / 手持 |
| 场景 | 影棚/场景图 | 卧室/车内/咖啡店/厨房 |
| 故事板手部特写 | 不要求 | ≥4 格 |
| negative_prompt | 通用 | 额外压制 studio/professional |

### 实现要点

完整复用八阶段共创漏斗，只替换两处：
1. **脚本阶段**：UGC 文案约束（禁用词汇表 + 第一人称强制 + "不完美"细节要求）
2. **人物板/故事板 prompt**：自拍构图锁定块（复用现有 `_subject_definition_block` 机制）

无新引擎，纯 skill 层工作。

### 验收标准
- 产出片通过 take_review 80 分闸门
- 脚本审查（`prompt_review`）能拦截企业腔文案
- 故事板自拍构图格数达标

---

## 3.3 创意变体矩阵（variant-matrix-testing）

### 已创建 Schema

**`schemas/variant-matrix.schema.json`** — 定义 hooks × actors × CTAs 笛卡尔积。

### 核心数据结构

```json
{
  "schema_version": 1,
  "base_segments": [
    {"id": "body-1", "text": "产品核心卖点", "video_type": 4, "duration": 8}
  ],
  "variant_axes": {
    "hooks": [
      {"id": "pain", "text": "用了两周，说三个我没想到的地方", "video_type": 4, "duration": 5},
      {"id": "contrast", "text": "买之前以为是XX，结果完全不是", "video_type": 4, "duration": 5},
      {"id": "scene", "text": "在XX的时候用了一下，发现一个细节", "video_type": 4, "duration": 5}
    ],
    "actors": [
      {"id": "actor-01", "cast_board_path": "actors/acme/cast_01.jpg", "voice_type": "warm-female"},
      {"id": "actor-02", "cast_board_path": "actors/acme/cast_02.jpg", "voice_type": "confident-male"}
    ],
    "ctas": [
      {"id": "soft", "text": "感兴趣可以看看", "video_type": 4, "duration": 3},
      {"id": "urgent", "text": "限时活动，抓紧", "video_type": 4, "duration": 3}
    ]
  },
  "ratio": "9:16",
  "model": "seedance-2.0",
  "budget_limit_usd": 15.0
}
```

### 处理逻辑

```python
# video_engine.py 新增函数
def expand_variant_matrix(matrix_path):
    """展开变体矩阵为 segments.json 列表。

    笛卡尔积：hooks × actors × ctas
    每个变体 = [hook_segment] + base_segments + [cta_segment]
    每个变体的 segments[0].urls 注入对应 actor 的 cast_board_path
    """
    import json
    with open(matrix_path) as f:
        matrix = json.load(f)

    hooks = matrix["variant_axes"]["hooks"]
    actors = matrix["variant_axes"].get("actors", [{"id": "default"}])
    ctas = matrix["variant_axes"]["ctas"]
    base = matrix["base_segments"]

    variants = []
    for hook in hooks:
        for actor in actors:
            for cta in ctas:
                variant_id = "v_%s_%s_%s" % (hook["id"], actor["id"], cta["id"])
                segments = [
                    {**hook, "id": "%s_hook" % variant_id,
                     "urls": [actor.get("cast_board_path")] if actor.get("cast_board_path") else []},
                    *[{**s, "id": "%s_%s" % (variant_id, s["id"])} for s in base],
                    {**cta, "id": "%s_cta" % variant_id,
                     "urls": [actor.get("cast_board_path")] if actor.get("cast_board_path") else []},
                ]
                variants.append({
                    "variant_id": variant_id,
                    "hook_id": hook["id"],
                    "actor_id": actor["id"],
                    "cta_id": cta["id"],
                    "segments": segments,
                })
    return variants
```

### 成本纪律

矩阵提交前给客户看"变体数 × 单条估算成本 = 总预算"确认页：

```python
# 确认页面计算
import cost_ledger
variants = expand_variant_matrix("variant_matrix.json")
per_video_est = cost_ledger._estimate_video_cost("seedance-2.0", 15)  # 假设 15s/条
total_est = len(variants) * per_video_est
if matrix.get("budget_limit_usd") and total_est > matrix["budget_limit_usd"]:
    raise ValueError("BUDGET_EXCEEDED: %d 变体 × $%.2f = $%.2f > 上限 $%.2f"
                      % (len(variants), per_video_est, total_est, matrix["budget_limit_usd"]))
print("确认：%d 个变体，估算总成本 $%.2f（每条 $%.2f）" % (len(variants), total_est, per_video_est))
```

### 输出

```python
# 变体报告
def generate_variant_report(variants, results, ledger_path):
    """生成 variant_report.md：变体对照表 + 每变体 take 评分 + 成本明细"""
    cost = cost_ledger.estimate_run_cost(ledger_path)
    # 按 variant_id 归集成本
    by_variant = {}
    for variant in variants:
        vid = variant["variant_id"]
        seg_costs = {s["id"]: cost["by_segment"].get(s["id"], 0) for s in variant["segments"]}
        by_variant[vid] = {
            "hook": variant["hook_id"],
            "actor": variant["actor_id"],
            "cta": variant["cta_id"],
            "cost": sum(seg_costs.values()),
            "take_score": next((r.get("take_score") for r in results if r.get("variant_id") == vid), None),
        }
    # 输出 markdown 表格
```

### 验收标准
- 笛卡尔积正确展开（hooks × actors × ctas 条数）
- 一次性 `--batch` 并行提交（墙钟≈单段）
- 每变体独立 OCR/take 闸门
- 报告含成本明细 + 评分对照

---

## 3.4 效果回流闭环（performance-feedback）

### 已创建 Schema

**`schemas/performance.schema.json`** — 定义投放效果数据结构。

### 核心数据结构

```json
// assets/<client>/performance.json
{
  "schema_version": 1,
  "client": "acme",
  "videos": [
    {
      "run_id": "20260803-120611",
      "delivery_sha256": "a1b2c3...",
      "channel": "douyin",
      "published_at": "2026-08-03T10:00:00+08:00",
      "metrics": {
        "views": 15420,
        "completion_rate": 0.42,
        "ctr": 0.031,
        "conversions": 17,
        "likes": 320,
        "comments": 45,
        "shares": 12
      },
      "variant_axis": {"hook_id": "pain", "actor_id": "actor-01", "cta_id": "soft"},
      "notes": "痛点钩子完播率明显高于反差钩子版"
    }
  ],
  "learnings": [
    {
      "insight": "15s 痛点钩子结构完播率比 30s 反差钩子高 40%",
      "evidence_run_ids": ["20260803-120611", "20260803-120612"],
      "applicable_stage": "hook"
    }
  ]
}
```

### Agent 集成（共创阶段引用历史数据）

在 `script-cocreation` skill 的意图/结构阶段，Agent 读取 `performance.json`：

```python
# skills/script-cocroduction.md 中新增引导规则
def load_performance_context(client):
    """读取客户历史投放数据，提取可引用的 learnings。"""
    import json, os
    path = os.path.join("assets", client or "default", "performance.json")
    if not os.path.isfile(path):
        return None
    with open(path) as f:
        data = json.load(f)
    learnings = data.get("learnings", [])
    if not learnings:
        return None
    # 返回格式化推荐语，供 Agent 在共创对话中引用
    return [
        "📊 历史数据参考：%s（来源 run: %s）" % (
            l["insight"], ", ".join(l.get("evidence_run_ids", [])))
        for l in learnings
        if l.get("applicable_stage") in ("intent", "structure", "hook", "cta")
    ]
```

### 交付后提醒

`pipeline.py create_delivery` 成功后，Agent 主动输出：

```
✅ 交付完成。发布后请回填效果数据到 assets/<client>/performance.json
   建议字段：channel, published_at, metrics.completion_rate, metrics.ctr
   下次共创时我会自动引用这些数据来优化推荐。
```

### 验收标准
- 第二次共创对话中 Agent 的推荐至少引用一条历史投放数据
- `performance.json` 通过 schema 校验
- learnings 数组非空时 Agent 必须引用

---

## 3.5 AI 披露合规开关（ai-disclosure-compliance）

### 价值
Meta/TikTok/YouTube 已要求或建议 AI 生成内容披露；这是客户法务刚需，且几乎零成本。

### 实现

**HyperFrames 场景 JSON 模板**（片尾 1s 角标）：

```json
{
  "resolution": [1080, 1920],
  "fps": 30,
  "duration": 1,
  "background": {"type": "transparent"},
  "brand": {"primary": "#666666"},
  "scenes": [
    {
      "text": "AI 生成内容",
      "start": 0,
      "end": 1,
      "preset": "fade",
      "size": 28,
      "pos": "lower",
      "color": "#999999"
    }
  ]
}
```

**多语言文案**（从 `brand/<client>/brand.json` 读取）：

```json
{
  "ai_disclosure": {
    "zh-CN": "AI 生成内容",
    "zh-TW": "AI 生成內容",
    "en": "AI-generated content"
  }
}
```

**pipeline.py 集成**（final 阶段后）：

```python
# final_edit.py 新增参数
def render_final(..., disclosure=False, disclosure_lang="zh-CN"):
    """合成成片，可选叠加 AI 披露角标。"""
    if disclosure:
        # 1. 用 hf_engine 渲染 1s 透明背景角标
        # 2. ffmpeg overlay 到成片最后 1s 右下角
        # 3. 不重新生成视频，纯叠加
```

**manifest 记录**：

```json
{
  "disclosure": {
    "applied": true,
    "style": "corner-badge",
    "duration_ms": 1000,
    "lang": "zh-CN",
    "text": "AI 生成内容"
  }
}
```

### 验收标准
- 开启后成片片尾含角标
- manifest 可审计
- 默认建议开启（合规默认安全）

---

## 3.6 已有优势强化

| 已有能力 | 强化方向 | 具体动作 |
|---|---|---|
| HyperFrames 字幕 | 业界 85% 社交视频静音播放 | captions 阶段改正式流程默认开启 |
| 音画一体 kling-v3 | 品牌声纹一致性 | `voice_type` 沉淀到 `brand/<client>/brand.json` |
| 事故文化 | 作为 SaaS 信任资产 | INC 模板产品化，季度复盘输出 |
| take 评审 80 分闸门 | 从内部质检变交付物 | 评分维度输出为客户可见的质量报告 |

---

# 第四部分：落地路线（v3 更新）

| 阶段 | 内容 | 状态/验证 | 已创建文件 |
|---|---|---|---|
| ~~第 1-2 周~~ | ~~P0/P1/P2 修复~~ | ✅ 已完成 | — |
| ~~第 3 周~~ | ~~成本账本 + 延迟 import 治理~~ | ✅ 已完成 | `scripts/cost_ledger.py`, `tests/test_no_inline_imports.py` |
| ~~第 3 周末~~ | ~~延迟 import 全量提升~~ | ✅ 已完成（22 模块 0 失败） | 8 个脚本修改 |
| ~~第 4-5 周~~ | ~~上帝文件拆分子模块~~ | ✅ 已创建 7 个子模块 | `video_models/prompts/tasks/guard.py` + `board_plan/prompts/confirm.py` |
| ~~第 5-6 周~~ | ~~业务层 4 个新模块~~ | ✅ 已创建并验证 | `variant_matrix.py`, `performance_feedback.py`, `ai_disclosure.py`, `derive_ratio.py` |
| 待做 | video_engine.py / storyboard.py shim 接线 | 子模块已就绪，shim 化（删除原函数、改 import）需 git 安全网 | — |
| 待做 | pipeline.py STAGES 扩展（derive stage） | derive_ratio.py 已就绪，pipeline 接线待做 | — |
| 待做 | cost_ledger.py 单价表校准 | 当前为估算值，需与实际网关计费核对 | — |
| 可用 | UGC 场景 skill | `skills/ugc-review/`, `skills/ugc-unboxing/` 已就绪 | SKILL.md × 2 |
| 可用 | Schema 契约 | `variant-matrix.schema.json`, `performance.schema.json` | schemas/ × 2 |

**原则不变**：代码改动走"事故 → 修复 → 回归测试 → 铁律沉淀"闭环；新业务走"确认闸门先行"哲学，不破坏架构一致性。

---

# 附录：v3 新增文件清单

| 文件 | 行数 | 用途 |
|---|---|---|
| `scripts/cost_ledger.py` | 183 | 成本账本：从 generation_ledger 事件估算成本，输出 retry_waste_ratio |
| `tests/test_no_inline_imports.py` | 132 | AST 扫描防回归：禁止业务模块函数级 import 核心模块 |
| `schemas/variant-matrix.schema.json` | 67 | 变体矩阵数据契约：hooks × actors × ctas 笛卡尔积 |
| `schemas/performance.schema.json` | 62 | 效果回流数据契约：投放指标 + learnings |
| `skills/ugc-review/SKILL.md` | ~80 | 第一人称测评 UGC 场景：禁用词汇表 + 自拍构图锁定 |
| `skills/ugc-unboxing/SKILL.md` | ~90 | 开箱测评 UGC 场景：九宫格板 UGC 扩展 + 手部特写锁定 |
