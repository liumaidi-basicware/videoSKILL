# Actor library template

每个正式客户使用 `actors/<client>/<actor>/` 保存数字人：

```text
actors/<client>/<actor>/portrait.png
actors/<client>/<actor>/meta.json
actors/<client>/<actor>/outfits/<outfit>/portrait.png
```

Momax 目录只是 demo，不是默认业务绑定。
